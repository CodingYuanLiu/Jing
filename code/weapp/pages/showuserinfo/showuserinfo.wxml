<!--pages/showuserinfo/showuserinfo.wxml-->
<view hidden="{{!error}}">network error</view>
<view hidden="{{error}}">
    <view class="user flex-wrp">
        <view class="avatar flex-item">
            <image class="userinfo-avatar" src="{{avatar_src}}" background-size="cover"></image>
        </view>
        <view class="user-info flex-item">
            <text style="font-size:40rpx; padding-top: 20rpx">{{user.nickname}}</text>
        </view>
    </view>
</view>
<view hidden="{{error}}">
    <i-panel title="个人信息">
        <i-input value="{{ user.username }}" disabled type="text" maxlength="10" title="用户名" disabled/>
        <i-input value="{{ user.nickname }}" disabled type="text" maxlength="10" title="昵称" disabled="{{!editing}}" />

        <i-input value="{{ user.signature }}" disabled type="text" title="个性签名" maxlength="50" disabled="{{!editing}}" />
        <i-input value="{{ user.phone }}" disabled type="number" title="联系电话" maxlength="11" disabled="{{!editing}}" />
    </i-panel>

</view>